# riviere-schema

Riviere JSON schema definition and validation utilities.

## Versioning

Keep the schema version and package major version aligned.

Do not include riviere-schema in breaking change commits (`!` or `BREAKING CHANGE`) - this automatically bumps the major version.
