{
  "name": "riviere-query",
  "targets": {
    "typedoc": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec typedoc --options packages/riviere-query/typedoc.json",
        "cwd": "{workspaceRoot}"
      },
      "dependsOn": ["build"]
    }
  }
}
