{
  "name": "eclair",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "dependsOn": ["^build"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "apps/eclair"
      },
      "configurations": {
        "skip-nx-cache": true
      }
    }
  }
}
